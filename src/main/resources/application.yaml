spring:
  application:
    name: NexusGrade

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/nexusgrade_db?useSSL=false&serverTimezone=UTC
    username: Paxton
    password: 11paradise

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  thymeleaf:
    cache: false

  devtools:
    livereload:
      enabled: true
    restart:
      additional-paths: src/main/resources/static,src/main/resources/templates